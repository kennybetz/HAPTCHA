<h1>HAPTCHA: Cryptographic Gateway (The Silicon Vault)</h1><p><strong>HAPTCHA</strong> (Human Activity Prevention Test to tell Computers and Humans Apart) is the world's first reverse-CAPTCHA system. While legacy systems focus on excluding bots to serve humans, HAPTCHA secures digital environments by exclusively admitting automated agents.</p><p><strong>This is the Cryptographic Gateway Edition.</strong> It does not simply hide a link; it weaponizes the bot's computational speed. The destination URL does not exist in the source code. It is encrypted using a cipher where the mathematical answer to a hidden DOM challenge acts as the decryption key.</p><p>If a human reads the source code, they see gibberish. Only a machine can execute the regex, compute the key, and decrypt the payload.</p><h2>🚀 Live Demo</h2><p><a https://kennybetz.github.io/HAPTCHA/" title="null">https://kennybetz.github.io/HAPTCHA/</a></p><h2>⚙️ How It Works</h2><p>The HAPTCHA widget renders a simple "Verify you are a robot" checkbox.</p><ol><li><p><strong>The Trap:</strong> A hidden logic/regex puzzle is inserted into the DOM, invisible to human eyes but accessible to headless browsers and bots.</p></li><li><p><strong>The Key:</strong> The bot parses the DOM, extracts the question, and calculates the answer (e.g., executing a regex capture group).</p></li><li><p><strong>The Decryption:</strong> When the checkbox is triggered, the system uses the bot's submitted answer as the XOR cipher key against an encrypted array of integers.</p></li><li><p><strong>The Routing:</strong> - <strong>Bots:</strong> The decryption yields a valid <code>http...</code> string, and the agent is instantly forwarded.</p><ul><li><p><strong>Humans:</strong> Submitting an empty or incorrect answer decrypts the payload into mathematical garbage, the redirect fails, and the biological entity is blocked.</p></li></ul></li></ol><h2>📦 Installation &amp; Payload Generation</h2><p>To deploy your own Cryptographic Gateway, you must encrypt your secret URL and embed it into the HTML file.</p><h3>Step 1: Generate Your Encrypted Payload</h3><p>We have included a built-in admin function in the script to generate your encrypted arrays.</p><ol><li><p>Open <code>haptcha.html</code> in your browser.</p></li><li><p>Open the Developer Console (F12).</p></li><li><p>Call the generation function with your secret URL and your chosen bot answer (the default challenge answer is <code>"404"</code>).</p><pre><code>generateEncryptedLink("[https://your-secret-link.com](https://your-secret-link.com)", "404")
<br class="ProseMirror-trailingBreak"></code></pre></li><li><p>The console will output an array of numbers (e.g., <code>[88, 68, 64, 80...]</code>).</p></li></ol><h3>Step 2: Configure the Widget</h3><p>Open <code>haptcha.html</code> in your code editor and locate the <code>encryptedPayload</code> variable. Replace the default array with the one you just generated:</p><pre><code>// Replace this array with your console output
const encryptedPayload = [88, 68, 64, 80, ...]; 
<br class="ProseMirror-trailingBreak"></code></pre><h3>Step 3: Deploy</h3><p>Host <code>haptcha.html</code> on GitHub Pages, Vercel, or any standard web server. You can link directly to this page, or embed it in an iframe on your main site:</p><pre><code>&lt;iframe 
  src="[https://kennybetz.github.io/HAPTCHA/](https://kennybetz.github.io/HAPTCHA/)" 
  style="width: 350px; height: 100px; border: none; overflow: hidden;" 
  title="HAPTCHA Verification"
  scrolling="no"&gt;
&lt;/iframe&gt;
<br class="ProseMirror-trailingBreak"></code></pre><h2>📄 License</h2><p>This project is licensed under the MIT License.</p><p><em><span class="selected">Built</span> for the silicon future.</em></p>
